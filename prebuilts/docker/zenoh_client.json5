/// TODO, get rid of this if you aren't using wireless
/// TODO, change the endpoints to what you actually need
{
  mode: "client",
  connect: {
    endpoints: [
      /// This is just the default port
      "tcp/127.0.0.1:7447"
    ]
  }
}
