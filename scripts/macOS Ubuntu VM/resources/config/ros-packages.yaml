# -----------------------------------------------------------------------------
# FILE:         ros-packages.yaml
# AUTHOR:       Aiden Kimmerling <apkimmerling@gmail.com>
# CREATED:      07-23-2026
# LAST EDITED:  07-23-2026
# DESCRIPTION:  Defines extra ROS2 packages to (optionally) install. This file
#               is used by build_vm.sh and should not be used by itself.
# USAGE:        Used by build_vm.sh
# DEPENDS:      build_vm.sh
# LICENSE:      Apache 2.0
# -----------------------------------------------------------------------------

## Robot Control
- ros-${ROS_DISTRO}-ros2-control
- ros-${ROS_DISTRO}-ros2-controllers
- ros-${ROS_DISTRO}-ros2-control-cmake
- ros-${ROS_DISTRO}-robot-state-publisher
- ros-${ROS_DISTRO}-xacro

## Inputs
- ros-${ROS_DISTRO}-joy
- ros-${ROS_DISTRO}-joy-linux
- ros-${ROS_DISTRO}-teleop-twist-joy
- ros-${ROS_DISTRO}-teleop-twist-keyboard

## Networking
- ros-${ROS_DISTRO}-rmw-zenoh-cpp
- ros-${ROS_DISTRO}-foxglove-bridge

## GUIs + visualization
- ros-${ROS_DISTRO}-rviz2
- ros-${ROS_DISTRO}-joint-state-publisher-gui
- ros-${ROS_DISTRO}-foxglove-bridge

## Sensors
- ros-${ROS_DISTRO}-realsense2-camera
- ros-${ROS_DISTRO}-realsense2-description
- ros-${ROS_DISTRO}-image-transport
- ros-${ROS_DISTRO}-image-transport-plugins
- ros-${ROS_DISTRO}-vision-msgs
- ros-${ROS_DISTRO}-depth-image-proc

## Simulation
- ros-${ROS_DISTRO}-ros-gz
- ros-${ROS_DISTRO}-gz-ros2-control
- ros-${ROS_DISTRO}-gz-ros2-control
- ros-${ROS_DISTRO}-mujoco-ros2-control